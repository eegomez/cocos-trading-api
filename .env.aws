# ==============================================================================
# AWS PRODUCTION ENVIRONMENT
# ==============================================================================
# Use on EC2 instance for AWS deployment
# CloudWatch for logs and metrics
# ==============================================================================

# Server Configuration
NODE_ENV=production
PORT=3000

# Database Configuration (RDS endpoint or EC2 PostgreSQL)
DB_HOST=<RDS_ENDPOINT>  # e.g., cocos-db.xxx.us-east-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=cocos_trading
DB_USER=<DB_USER>
DB_PASSWORD=<DB_PASSWORD>
DB_MAX_CONNECTIONS=20

# Logging Configuration
# LOG_LEVEL options: fatal | error | warn | info | debug | trace
LOG_LEVEL=info
LOG_PRETTY=false
LOGGER_TYPE=cloudwatch  # cloudwatch = send logs to AWS CloudWatch

# CloudWatch Logs Configuration
AWS_REGION=us-east-1  # Your AWS region
CLOUDWATCH_LOG_GROUP=/aws/cocos-trading/api
CLOUDWATCH_LOG_STREAM=main

# Metrics Configuration
METRICS_TYPE=cloudwatch  # cloudwatch = send metrics to AWS CloudWatch

# CloudWatch Metrics Configuration
CLOUDWATCH_METRICS_NAMESPACE=CocosTrading

# AWS Credentials (Optional - use IAM role instead)
# If running on EC2 with IAM role, these are NOT needed
# AWS_ACCESS_KEY_ID=<your-key>
# AWS_SECRET_ACCESS_KEY=<your-secret>
